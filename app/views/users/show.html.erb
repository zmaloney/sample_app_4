<% provide(:title, @user.name) %>
<!-- use the @user object we've defined in the corresponding show action in the Users controller. --> 
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>
<p><%= @user.email %></p>